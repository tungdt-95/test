import React from 'react';

const Cart = () => {
    return (
        <div>
            <button>Cart</button>
            {
                cart
            }
        </div>
    );
};

export default Cart;