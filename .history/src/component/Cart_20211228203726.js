import React from 'react';

const Cart = () => {
    return (
        <div>
            <button>Cart</button>
            {
                cart.map
            }
        </div>
    );
};

export default Cart;