import * as actionTypes from "./actions-type";


const Initstate = {
    product:[],
    cart:[],
    currentItem: null,
}

const Reducer = (state,action) => {
    switch (action.type) {
        case a:
            
            break;
    
        default:
            break;
    }

}