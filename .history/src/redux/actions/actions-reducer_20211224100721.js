import * as actionTypes from "./actions-type";


const Initstate = {
    product:[],
    cart:[],
    currentItem: null,
}

const Reducer = (state,action) => {
    switch (ac) {
        case value:
            
            break;
    
        default:
            break;
    }

}